---
  - name: ensure user manager created and member of group wheel
    user: name=manager groups=wheel

  - name: ensure keys added
    authorized_key: user=manager key="{{ lookup('file', '.ssh/manager.pub') }}"